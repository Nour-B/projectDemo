import { Component } from '@angular/core';

@Component({
    selector: 'my-app',
    template: `
        <div class="container">
         <hero-detail></hero-detail>
        </div>
    `
})
export class AppComponent {


}
